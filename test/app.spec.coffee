describe 'app', ->
    it 'should connect to Users Google Drive Account', ->
    
    it 'should read JSON from Sheet file OYL/Possessions', ->
        
    it 'should do a lookup of UPCs on upclookup.org of UPC column', ->
    
    it 'should prompt action when row data from UPC site has changed', ->
        
    it 'should be able to add new columns to the sheet from UPC data', ->
        
    it 'should be able to input data from UPC site into cells', ->
        
    it 'should be able to allow custom row data to be added by user', ->
        
    it 'should be able to get price data from Amazon, eBay, CraigsList, Walmart', ->